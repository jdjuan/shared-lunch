import { MatToIterablePipe } from './mat-to-iterable.pipe';

describe('MatToIterablePipe', () => {
  it('create an instance', () => {
    const pipe = new MatToIterablePipe();
    expect(pipe).toBeTruthy();
  });
});
